import 'package:flutter/material.dart';

class Spawn extends StatelessWidget {
  const Spawn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
